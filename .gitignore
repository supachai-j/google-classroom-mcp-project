__pycache__/
*.py[cod]
.venv/
.env
*.egg-info/
dist/
build/
.ruff_cache/
.pytest_cache/

# OAuth client + refresh token — NEVER commit
secrets/credentials.json
secrets/token.json

# Local grade exports + per-cohort grading artifacts (contain student PII)
exports/
CPE3326_*.xlsx
*.xlsx

# Per-cohort config + raw reference material (contain student PII / restricted content)
data/
